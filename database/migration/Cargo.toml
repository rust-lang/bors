[package]
name = "migration"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
tokio = { workspace = true }
sea-orm-migration = { version = "0.11.0", features = ["sqlx-sqlite", "sqlx-postgres", "runtime-tokio-rustls"] }
